# Базовый URL публичного API каталога (apps/api).
# Для локальной разработки — API из docker compose.
# Если переменную не задать, сайт пойдёт в прод: https://ilalex-vinyl-api.onrender.com
NUXT_PUBLIC_API_BASE=http://localhost:3001
