{
  "name": "ilalex-vinyl-monorepo",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "yarn workspace @ilalex-vinyl/web dev",
    "build:web": "yarn workspace @ilalex-vinyl/web build",
    "dev:api": "cd apps/api && yarn dev",
    "build:api": "cd apps/api && yarn build"
  }
}
